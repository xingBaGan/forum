import Vue from 'vue'
import Vuex from 'vuex'
import sourceDate from '@/data'
Vue.use(Vuex)

export default new Vuex.Store({
  state: sourceDate,
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
